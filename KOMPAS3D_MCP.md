# Kompas3D MCP

**MCP-сервер для КОМПАС-3D v24:** ИИ-агент строит и проверяет настоящие детали и сборки через COM,
а сервер отвечает только тем, что действительно измерил на модели.

*MCP server that drives a real КОМПАС-3D v24 over COM. Every claimed capability is backed by a
measured acceptance run, not by the presence of an API.*

## Для чего это

Системное проектирование в КОМПАС-3D обычно автоматизируют макросами, где «вызов прошёл» легко
выдать за «модель изменилась». Этот сервер устроен так, чтобы такого разрыва не было: у каждой
мутирующей операции есть проверяемое последствие — объём, число тел и граней, перечитанный параметр
признака, состояние после `save → close → reopen`. Если вызов сработал, а измерение этого не
подтвердило, ответ помечается более низким уровнем проверки, а не «успех».

## Как устроено

```
MCP-клиент (stdio)
   │
KompasMcp.Host      — контракты инструментов, строгая валидация аргументов, журнал,
   │                  реестр ссылок и ревизий; COM не используется вообще
   │ именованный канал
KompasMcp.Worker    — один STA-поток с настоящим pumping message loop; весь КОМПАС живёт здесь
   │
KompasMcp.Api5Adapter — единственный проект, который ссылается на типы КОМПАСа (API5 + точечно API7)
```

Один владелец COM-сеанса, одна STA-очередь на вызовы обоих API, общий реестр документов, ссылок и
ревизий — подробности в [`docs/adr/`](docs/adr/). Доступ к диску ограничен списком корней из
конфига; операции с путями вне их отклоняются до обращения к КОМПАСу.

**Несколько запросов могут быть в полёте одновременно.** Это не удобство, а требование: длинная
мутация продолжает выполняться в фоне **после** возврата вызова (режим `running`), и `kompas_health`
обязан отвечать, пока она идёт. Поэтому канал «запрос/ответ» держит **единственного читателя** и
раздаёт кадры адресатам по идентификатору запроса, а запись сериализована. Прежняя редакция
сериализовала запись, но запускала цикл чтения на каждого вызывающего — два одновременных вызова
переплетали байты одного канала (измерено 18.09.2026 живым клиентом; разбор — `docs/acceptance/
B2_MCP_INTEGRATION.md` §4.4, доказательство наряда в публичный набор не входит, см. ниже). Класс
покрыт штатной пробой [`scripts/probe-concurrent-calls.py`](scripts/probe-concurrent-calls.py) с
отрицательным контролем.

## Что делает сейчас

50 инструментов (схемы — [`schemas/`](schemas/), они же используются для валидации аргументов):

| Группа | Инструменты |
|---|---|
| Сеанс и документы | `kompas_connect`, `kompas_disconnect`, `kompas_health`, `kompas_get_context`, `kompas_create_document`, `kompas_open_document`, `kompas_save_document`, `kompas_close_document`, `kompas_list_documents` |
| Эскизы | `kompas_create_sketch`, `kompas_edit_sketch`, `kompas_finish_sketch`, `kompas_get_sketch_status`, `kompas_set_sketch_plane`, `kompas_list_sketch_entities`, `kompas_edit_sketch_entity` |
| Опорная геометрия | `kompas_create_aux_geometry`, `kompas_list_aux_geometry`, `kompas_update_plane` |
| Твёрдое тело | `kompas_extrude`, `kompas_rotated`, `kompas_hole`, `kompas_fillet`, `kompas_chamfer`, `kompas_boolean`, `kompas_cut_by_plane`, `kompas_split`, `kompas_reposition`, `kompas_rebuild`, `kompas_loft`, `kompas_sweep`, `kompas_shell` |
| Массивы | `kompas_pattern_grid`, `kompas_pattern_circular`, `kompas_pattern_mirror`, `kompas_get_pattern` |
| Признаки | `kompas_list_features`, `kompas_get_feature`, `kompas_update_feature`, `kompas_set_feature_suppressed`, `kompas_delete_feature` |
| Геометрия и измерения | `kompas_list_bodies`, `kompas_read_topology`, `kompas_measure`, `kompas_resolve_selection`, `kompas_probe_units` |
| Обмен | `kompas_export_step`, `kompas_import_step`, `kompas_export_image` |
| Самоописание | `kompas_capabilities` |

**Поправка 23.09.2026.** В прежней редакции этого раздела стояло «40 инструментов», и таблица
перечисляла ровно 40 имён. Измерено в тот день: `src/KompasMcp.Host/Catalog/ToolCatalog.cs`
регистрирует **50** инструментов, `schemas/` в индексе публикации содержал **44** файла, а
`KompasMcp.Host.exe --emit-schemas` записывает **50**. Не хватало ровно десяти — тех, что добавлены
в таблицу выше (`kompas_set_sketch_plane`, `kompas_list_sketch_entities`, `kompas_edit_sketch_entity`,
`kompas_create_aux_geometry`, `kompas_list_aux_geometry`, `kompas_update_plane`, `kompas_loft`,
`kompas_sweep`, `kompas_shell`, `kompas_export_image`); лишних в таблице не было ни одного
(сверка `comm` по отсортированным спискам: 10 в схемах и нет в документе, 0 в документе и нет в
схемах). Схемы добраны из вывода самого Host — шесть файлов побайтово совпали с его записью.
Число «40» здесь было **текущим состоянием**, а не датированным замером, поэтому исправлено, а не
оставлено с пометкой.

Проверено на живом КОМПАС-3D v24 интеграционным прогоном
([`scripts/mcp-smoke.py`](scripts/mcp-smoke.py)) на бинарях поставки `artifacts/publish-b4-20260920`:
**полный прогон 630 строк, 0 отказов** (5216 вызовов);
групповые прогоны полным не заменяются — вращение 108, группа B3 84, группа B3M 122 (жизненный цикл
признака «Изменение положения»), группа B3L 25, группа B3C 34, группа B4 129 (жизненный цикл
массивов), фаска 60, родное отверстие 59, скругление 50, выдавливания 39, определённость эскиза 35,
контракт 19, группа DL 33 (сохранённость документа и политика закрытия `refuse`/`save`/`discard`);
все PASS. Полный и групповые **не складываются**: групповые — те же проверки по частям. Числа — из
массивов `rows` отчётов
(`scratch/mcp-smoke/delivery-b4-20260920/*.json`), не из сводки на экране. Покрыто:
создание и перечитывание документов, эскизы, выдавливание во всех режимах с явным выбором целевого
тела, вращение (в том числе полный оборот 360° всеми тремя видами), родное отверстие трёх режимов,
скругление и фаска (включая способ «расстояние и угол» через API7), булевы операции, разделение и
отсечение плоскостью, **перенос и поворот тела с достоверным чтением параметров преобразования —
в том числе с переоткрытого документа** (произвольная ось, ось через точку вне начала координат,
последовательность «перенос → поворот»), **массивы — по сетке, круговой и зеркальный, по копированию
и операций, и тел**, правка параметров существующего признака, жизненный цикл признака после
повторного открытия, перестроение, STEP, политика путей, идемпотентность и конфликт
ревизий. Текущая очерёдность и состояние каждой возможности —
[`coverage/solid-v24/matrix.md`](coverage/solid-v24/matrix.md).

Приёмка запускается **по бинарям поставки**, а не по отладочной сборке, и итог собирается в
машинный паспорт `docs/acceptance/delivery-b4-20260920/delivery-passport.json`
(`scripts/make-delivery-passport.py`). Он различает два утверждения, которые легко спутать:
`client_entry` — что запись сервера в конфигурации клиента запускается, и `client_acceptance` —
что вызовы инструментов из самого клиента дали результат. Одно не заменяет другое. На поставке B4
`client_entry` читается **FAIL**: запись клиента указывает на более раннюю поставку, где инструментов
**36** против **40**; это ожидание действия пользователя, а не результат прогона.
**Пометка 23.09.2026:** «40» здесь — число инструментов **на момент поставки B4**, а не текущее;
текущее измеренное — 50 (см. раздел «Что делает сейчас»). Замер B4 не переписан: он верен для B4.

## Чего от него не ждать

- Он **не** редактирует произвольную операцию КОМПАСа. Возможность подтверждается измерением, а не
  наличием типа в interop; неизмеренное возвращается как `CAPABILITY_UNAVAILABLE` или `blocked_api`
  с указанием, что именно уже проверено и чего не хватает.
- Практический выпуск **не закрыт**: из 54 обязательных режимов профиля `mechanical-core-v1` закрыты
  по всем критериям приёмки **47** (`mcp_verified`), общих зависимостей — **2** из **16**;
  остальные доведены до разного числа действий из десяти. Число закрытых строк — следствие измерения,
  а не цель: `47/54` означает 47 строк, где все десять действий подтверждены, а не «47 работающих
  возможностей». Действующее число — замер 20.09.2026, 13:05 (`scripts/emit-coverage-matrix.py`,
  `project-progress.mjs`, паспорт `docs/acceptance/delivery-b4-20260920/delivery-passport.json`);
  прежние «37/54», «35/54», «1/16» и «51.4 %» — измерение своего момента и в датированных
  разделах не переписываются. Уровень десяти строк B4 поднят с `runtime_verified` до `mcp_verified`
  20.09.2026: он **отставал** от собственных действий строки (все десять `verified`), и это нашла
  штатная сверка трёх записей `scripts/record-b2-rows.py --check`, а не глаз.
- **Массивы** покрыты тремя видами (по сетке, круговой, зеркальный) и двумя предметами копирования
  (признаки и тела). Две границы названы прямо, а не умолчаны: свойство `save_initial_objects`
  работает **только** для операции `o3d_mirrorAllOperation` — у прочих операций зеркального
  копирования возможность скрыть экземпляры справкой отсутствует, и запись `false` на операции 48
  читается обратно как `true` без изменения геометрии; а копирование ТЕЛ — это **другой** признак в
  дереве (свой номер типа 528/529 против 35/36), хотя отображается он тем же именем.
- У **переноса и поворота тела** читается не всё, и это названо, а не умолчано: вид преобразования,
  вектор переноса, направление оси и угол читаются — в том числе с переоткрытого документа, — а
  **точка оси не публикуется**: документированного члена для неё нет, и она не является свойством
  размещения (поворот вокруг любой точки одной прямой даёт то же размещение). Признаки, созданные
  прежней, матричной редакцией, **не переводятся молча**: продукт отказывает, называя все пять полей.
- Нужна установленная и лицензированная КОМПАС-3D v24 x64: сервер работает с реальным приложением и
  не эмулирует CAD.

## Требования и запуск

Windows x64 · .NET SDK 10 (`global.json` закрепляет 10.0.401) · КОМПАС-3D v24 x64.

```powershell
dotnet build KompasMcp.sln -c Debug -p:Platform=x64 -p:KompasRoot="<корень установки КОМПАС-3D v24>"
dotnet build src\KompasMcp.Host\KompasMcp.Host.csproj -c Debug -p:Platform=x64 -p:KompasRoot="<тот же корень>"
```

Interop берётся из каталога установки и **не копируется** в выходную папку, поэтому вендорные
сборки в репозиторий не входят. Если КОМПАС не найден, сборка адаптера падает с внятным
сообщением — заглушки COM-слой не имеет.

**Сборка в облачном CI невозможна, и это свойство проекта, а не упущение.** Раннеру негде взять
установленный и лицензированный КОМПАС-3D v24: `build/KompasInterop.props` намеренно отказывает
сборке без `Interop.Kompas6API5.dll`, а не собирает заглушку. Проверка изменений — на машине с
установленным КОМПАС; автоматические прогоны возможны только там.

Сервер говорит по MCP поверх stdio:

```powershell
src\KompasMcp.Host\bin\x64\Debug\net10.0-windows\KompasMcp.Host.exe --config config\kompas-mcp.local.json
```

Конфигурация копируется из `config/kompas-mcp.example.json`: там roots доступа к диску (единственная
граница между сервером и файловой системой), лимиты очереди и бюджеты операций. Локальный файл
вида `*.local.json` в репозиторий не попадает и в поставку не уезжает.

Для клиента берётся не отладочная сборка, а поставка (`scripts\publish.ps1 -Configuration Release`,
каталог `artifacts\publish`), и её бинари проверяются своими приёмочными прогонами:
`python scripts\verify-delivery.py --delivery artifacts\publish-<дата>` сверяет контракт
опубликованного Host с приложенными схемами, а `python scripts\mcp-smoke.py --host <путь>` прогоняет
приёмку по бинарям поставки.

Модульные тесты идут без КОМПАСа:

```powershell
dotnet test tests\Unit\KompasMcp.Unit\KompasMcp.Unit.csproj -c Debug -p:Platform=x64
```

Приёмочные сценарии (`scripts/mcp-smoke.py`, `scripts/mcp-visibility-test.py`) запускают реальный
КОМПАС невидимым экземпляром и завершают его сами — одновременно допустим только один сеанс.

## Документация

| Где | Что |
|---|---|
| [`docs/INDEX.md`](docs/INDEX.md) | карта документации |
| [`docs/STATUS.md`](docs/STATUS.md) | текущее состояние, измеренные числа, известные отказы |
| [`docs/API_COMPLIANCE.md`](docs/API_COMPLIANCE.md) | сверка вызовов API с официальной справкой КОМПАС v24: два вердикта на маршрут и что исправлять ([машиночитаемая таблица](docs/API_COMPLIANCE.json)) |
| [`docs/adr/`](docs/adr/) | решения по COM-сеансу, проверке результатов и границе API5/API7 |
| [`docs/operator-guide/`](docs/operator-guide/) | сборка, запуск, диагностика |
| [`coverage/solid-v24/`](coverage/solid-v24/) | каталог возможностей, матрица покрытия, план выпуска |

## Что намеренно не входит в публичный репозиторий

Документ выше называет по имени артефакты наряда, которых в этом репозитории нет. Это не битые
ссылки, а названные адреса — перечислим их один раз, чтобы читателю не пришлось искать:

- `docs/acceptance/` — доказательства прогонов и приёмки: журналы, паспорта, реестр прогонов.
  Утверждения документов опираются на них, но сами доказательства остаются в рабочем дереве
  исполнителя, а в публикацию не переезжают.
- `artifacts/`, `scratch/` — собранные поставки и рабочие данные прогонов (модели, экспорты, журналы).
  Поставки воспроизводятся из исходников командой `scripts/publish.ps1`, а модели заказчика не
  публикуются ни в каком виде.
- `reference/` — справочные материалы прежнего проекта EH70: жёсткие локальные пути, для сборки,
  запуска и приёмки не требуются.
- `test/` — модели, по которым велась работа.
- `config/*.json`, кроме `config/kompas-mcp.example.json` — локальные пути этой машины.
- `docs/progress/` — дашборд наряда, производный от приборов.
- Приборы `tools/` — кроме `KompasMcp.P0Probe` и `KompasMcp.Api7Probe`: первый нужен решению для
  сборки, на `Late.cs` второго опирается страж границы позднего связывания в наборе тестов.
  Каталог `scripts/` публикуется целиком: без приборов читатель не может воспроизвести ни одну из
  заявленных здесь проверок.

Точный список того, что публикуется, задан `.gitignore` признаком роли, а не перечнем имён, и
проверяется стражем [`scripts/verify-publish-set.py`](scripts/verify-publish-set.py) по **индексу**
git (публикуется индекс, а не рабочее дерево).

**Как читать ссылки на эти артефакты.** Документы ниже называют их по имени — `scratch/…`,
`artifacts/…`, `docs/acceptance/…`. Это не «ссылки, которые не открылись»: это указание на
доказательство, которое читатель **воспроизводит**, а не открывает. Приборы, которые его создают,
опубликованы (`scripts/mcp-smoke.py`, `scripts/verify-delivery.py`, `scripts/make-delivery-passport.py`
и остальные), поэтому прогон на своей установке КОМПАС-3D v24 даёт те же файлы с теми же именами.
Там, где документ называет путь без такой возможности — например, снимок состояния наряда, —
это сказано прямо, а не оставлено на догадку.

Документы и комментарии в репозитории — на русском: он ведётся в связке с заказчиком, формулировки
приёмки имеют юридический для проекта смысл.
