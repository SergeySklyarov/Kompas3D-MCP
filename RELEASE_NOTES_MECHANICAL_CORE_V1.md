# Релизные заметки: `mechanical-core-v1` — практическое ядро твердотельного моделирования

**Профиль:** `coverage/solid-v24/release-profiles/mechanical-core-v1.json` («Практическое твердотельное
моделирование v24», редакция 1.1).
**Целевая среда:** КОМПАС-3D **v24.0.0.2799 x64**.
**Дата заметок:** 21.09.2026, 19:1x по часам машины (**UTC+2**, CEST). Даты в проекте — по часам
машины; расхождение с московским календарём в пределах часа возможно.
**Наряд:** `FINAL_RELEASE_AND_CLIENT_HANDOFF_DEVELOPER_PROMPT.md`.
**Обновлено 21.09.2026, 20:5x** нарядом `JOURNAL_SHARING_FIX_DEVELOPER_PROMPT.md`: поставка и **хеш
Trust в §4 устарели** — действующие числа в **Приложении Б**. Отчёт по тому наряду —
`JOURNAL_SHARING_FIX_REPORT_20260921.md`.

**Правило этих заметок:** каждое число снято прибором или прочитано из файла **в этом сеансе**, и
прибор назван рядом. Числа, взятые из артефакта, названы вместе с артефактом. Ничего «по памяти».

---

## 1. Что такое этот релиз

Практический выпуск профиля `mechanical-core-v1`: полезный набор твердотельного моделирования,
доведённый до состояния «пользователю осталось два действия» — выдать Trust коннектору и открыть
новую сессию.

| Что | Измерено | Чем |
|---|---|---|
| Обязательный объём | **COMPLETE**: режимов **54/54**, зависимостей **15/15**, открытых **0** | `scripts/acceptance-levels.py` |
| Готовность выпуска | **100 % · 54/54 · 15/15** | `scripts/project-progress.mjs` |
| Строки матрицы покрытия | 74: **56** «Подтверждено», 15 «Недостаточно проверки», 3 «Не реализовано» | `docs/API_COMPLIANCE.json` |
| Опубликованные инструменты | **50**; схем **50**, и все 50 совпадают с проводом и по именам свойств, и по форме | `docs/API_COMPLIANCE.json` |
| Поставка | `artifacts/publish-sketchplane-20260921` — 100 файлов, 7.5 МБ | `scratch/mcp-smoke/sketchplane-20260921/package-check.json` |
| Прогоны на бинарях поставки | full **1086/0**, f08 **301/0**, mania **43/0**, dep **80/0**, image **37/0** отказов | `scratch/mcp-smoke/sketchplane-20260921/*.json` |
| Аудит | инструменты без присутствия в матрице — **0**; ссылки документации **207/207** годных, битых 0 | `docs/API_COMPLIANCE.json`; `scratch/_check_compliance_links.py` |
| Притязания строк | **71 из 71** полностью подтверждены, приписок 0, пробелов 0 | `scratch/_verify_matrix_claims.py` |
| Сторож действий | **33 из 33**, сторож доказан, отказов на реальных данных 0 | `scratch/_verify_action_guard.py` |
| Линтеры | rc=0: иероглифов 0, смешанных алфавитов 0 | `scripts/lint-cjk.py`, `scripts/lint-mixed-script.py` |
| `KOMPAS.exe` | **0** процессов — сеансы КОМПАС этим нарядом не поднимались | `tasklist` |

**Знаменатель зависимостей — 15**, и это решение заказчика 21.09.2026: требование
`dep.bodies.remove_selected` (минимум SM-25) **снято** как невыразимое в поставленной сборке
24.0.0.2799 — типов `IBodySurfaceRemovers` / `IBodySurfaceRemover` в сборке нет вовсе (`TYPE_NOT_FOUND`,
прибор `tools/KompasMcp.InteropScan`), тогда как страницы справки маршрута существуют. Запись снятия —
`coverage/solid-v24/catalog.json`, поле `removed_requirement_note`. **Удаление требования не является
реализованной функцией**, и снятие отражено в знаменателе, а не спрятано.

**Хеши поставки** (sha256):

| Файл | Хеш | Откуда |
|---|---|---|
| `KompasMcp.Host.exe` | `d6517d8f6d214885a02a9f3f68642cd7e4f941ae1a39a1f447051f2b1abf34e1` | `package-check.json` |
| `KompasMcp.Host.dll` | `405e2ba551188215cdaf8d8a4ef32b17fd1465ed78f079a1dbe86fd9bd6ff32d` | `docs/API_COMPLIANCE.json` |
| `KompasMcp.Worker.dll` | `eb877a53b28670b688dcc4b9f96fe6fdd3b6f8c1bd871c6c3b58e7c3f3508e55` | там же |
| `KompasMcp.Api5Adapter.dll` | `5303dee82bfba1b476aec58389deb6953617f6d07dd1e36c4c788334ca47a5e8` | там же |

---

## 2. Чем релиз НЕ является

- **Не полная v1 по ТЗ.** Сборки, BOM/DXF и производственные пакеты, резьбы, оформление чертежей в
  этот выпуск не входят и не обещаны.
- **Три плановых режима вне выпуска** (состояние «Не реализовано», основание — «ни одно действие
  строки не начато»): `SM-01.primitive_box`, `SM-04.boss`, `SM-15.union.mode_save_base_copy`. В
  знаменатель 54 они не входят и сделанными не объявляются.
- **`WMF`/`EMF` не публикуются осознанно**; в требования не включались.
- **Метка версии сервера остаётся `0.1.0-preview`** (`src/KompasMcp.Host/Program.cs:24`).
  Переименование в 1.0.0 — отдельное решение заказчика, связанное с полной v1, и в этом релизе НЕ
  делается: оно меняет хеши Host и требует пересдачи, не добавляя доказательств.

---

## 3. Названные остатки

**3.1. Присутствие трёх инструментов в матрице — ЗАКРЫТО, способ назван.**
Ворота §2 наряда измерены прибором `scratch/_release_presence_gate.py` по **журналу вызовов** прогона
`sketchplane-20260921` (строки хранят окна индексами). У каждого из трёх инструментов нашлась
PASS-строка, чьё окно несёт **успешный** вызов и чьё действие этим вызовом обеспечено:

| Инструмент | Строка | Вызов (измерено) |
|---|---|---|
| `kompas_create_aux_geometry` | `dep.refs.axes` | `DEP.DAX.02.create`, `mode=by_2_points`, succeeded, ревизия 4 → 5 |
| `kompas_update_plane` | `dep.refs.planes` | `DEP.DPL.04.edit`, succeeded, ревизия 8 → 9 (+2 отказа `INVALID_ARGUMENT` как контроль) |
| `kompas_edit_sketch_entity` | `dep.sketch.entities` | `DEP.DSE.04.edit`, succeeded, ревизия 3 → 4 (+1 `GEOMETRY_FAILED` как контроль) |

Объявлены они **по одному имени на строку** в тексте `adapter_route` (наряд §2), записью через прибор
`scratch/_declare_release_routes.py` — **слиянием**, а не заменой: прежние тексты строк остались на
месте дословно, и сторож назвал бы потерю (доказан на настоящей потере, три половины). Счётчик
`tools_without_matrix_presence` стал **0** (было 3); присутствие: своей строкой **43**, объявленным
маршрутом **15** (было 12); контроль присутствия из трёх половин — **доказан** (`True`).

**Названо, а не умолчано:** у этих трёх присутствие держится **только текстом `adapter_route`**
(`tools_only_in_adapter_route` = ровно эти три имени) — структурного поля `*_route` у них нет, и
именно этот способ и был выбран нарядом. И `kompas_create_aux_geometry` вызывается успешно ещё и в
окнах `DEP.DPL.02.create` и `DEP.DPT.01.create`; имя объявлено **один раз**, поэтому «счётчик 0» не
следует читать как «каждый режим инструмента заявлен».

**3.2. Мониторинг флейка `MANIA.*` продолжается.**
Сигнатура: `MANIA.20.suppress_restore`, подавленный объём `133541.367970` при ожидании `132771.677754`
— лишняя величина **769.690210 мм³** ≈ π·7²·5 (Ø14 сквозь стенку). Падающий отчёт сохранён:
`scratch/_auxs-mania20-fail/full-mania20-fail-20260921.json` (1086 строк, 1 FAIL, sha256
`dfb49782ed7d415217ecc1dfcd1ddc878796de1563257f7a820a3e3eba46be20`). **В финальном прогоне флейк НЕ
воспроизвёлся**: `mania.json` 43/0 и `full.json` 1086/0 на бинарях поставки. Проверка не ослаблена и
не снята; повтор отказа открывает находку.

**3.3. Управление видом не реализовано.** `IViewProjection7` (SetMatrix3D/Update/Delete)
**документирован**, но в маршрут выпуска не входит (`coverage/solid-v24/catalog.json`; отдельный
будущий наряд). Следствие названо прямо: `kompas_export_image` снимает **текущее** состояние окна
сервера, и ориентацию модели снимок сам по себе не подтверждает.

**3.4. Метка версии сервера — `0.1.0-preview`** (см. §2): переименование в 1.0.0 есть отдельное
решение заказчика и в этом релизе не делается.

**3.5. Паспорт ждёт клиентскую приёмку.** `docs/acceptance/delivery-sketchplane-20260921-plane/delivery-passport.json`
(mtime 21.09.2026 18:35, **этим нарядом не переиздавался**): `package` **PASS** (100 файлов),
`measured_binaries` PASS, `functional_acceptance` PASS, `mandatory_scope` **COMPLETE**,
`rows_group_runs` 461, но **`transport_and_delivery` FAIL** — операнды `client_entry` и
`client_acceptance` в состоянии `not_run`. Это последний FAIL паспорта, и закрывается он шагом
пользователя из §4.

**3.6. Отчёт прибора доверия теперь ложится в каталог прогона — и это измерено.**
Дефект №25 исправлен (§Приложение А): умолчание `--out` больше не ведёт в каталог поставки
18.09.2026, а берётся из источника аудита. **Следствие названо:** прогон без `--out` кладёт
`mcp-approval-check.json` в каталог прогона, и набор имён строк приёмки вырос с 1061 до **1066**
(строки `A01`…`A05`). Ни одно из этих имён не привязано ни к одной строке матрицы, окна вызовов у них
нет, и **ни один показатель аудита от них не зависит**: пересобранный аудит побайтово равен
прежнему, кроме секунд свежести (сверено сравнением разобранных объектов).

**3.7. Файл сценария первой сессии отставал от финальной поставки — исправлено, расхождение названо.**
`IMAGE_CLIENT_CONTINUATION_PROMPT.md` был написан под **предыдущую** поставку
(`publish-image-20260921`, 49 инструментов) и нарядом §4.4 назначен сценарием финального сеанса. Файл
приведён к финальной поставке **датированным блоком** (прежние таблицы оставлены читаемой историей):
поставка, хеши, **50** инструментов, хеш записи, каталоги отчётов и приёмки, ключи переиздания
паспорта. Названо и то, что **не** сошлось: в таблице «Что считать успехом» четыре величины не
подтверждаются измеренным на финальной поставке — пропил (измерено `515.177025`, в таблице
`515.177019`), полость оболочки (`59273.699225` против `59273.699318`), отверстия ушей (`3078.760801`
против `3078.760770`), а величина цековки `3194.999729` в отчётах прогона **не встречается вовсе**.
Оперативная сверка в сеансе — по ИЗМЕРЕННЫМ.

---

## 4. Подключение: пользователю осталось два действия

Запись `kompas` в `<профиль>\.workbuddy-ai\mcp.json` **уже указывает на финальную поставку**
(наряд §3, правка сделана 21.09.2026 19:06; резервная копия прежней записи —
`scratch/client-config-backup-20260921.json`). Изменилась **одна строка** — `command`; `args` и
`timeout` не тронуты (сверено разбором обеих записей и построчным дифом).

**Хеш записи (снят прибором, а не пересчитан в оболочке):**

```
b4962263417925e837f70cd670a5fdaaf1c16b2c9643d0e28871f0e99d41f2e6
```

Материал хеша (печатает сам прибор, поле `hash_material`):

```
<репозиторий>\artifacts\publish-sketchplane-20260921\KompasMcp.Host.exe|--config,<репозиторий>\config\kompas-mcp.local.json|
```

Состояние по прибору `python scripts\verify-mcp-approval.py` (21.09.2026, 19:0x): **A01–A03 PASS**,
**A04/A05 — «ожидает Trust»**: подтверждения под новым хешем в хранилище нет, а четыре прежних
подтверждения выданы под другими хешами. Это **ожидаемое** состояние, а не поломка.

**Действие (а). Выдать Trust.** Открыть WorkBuddy AI → панель **«Connector Management»** → кнопка
**«Custom connectors»** (иконка «+» в шапке панели) → окно **«MCP Server Management»** → карточка
`kompas` → текст «This MCP server requires your trust before first connection.» → кнопка **«Trust»**.
Второй вход в то же окно: панель плагинов → вкладка Skills → кнопка «MCP Servers» в правом верхнем
углу (показывается не всегда, поэтому первый путь надёжнее). Сверить показанный хеш с числом выше.
Хранилище подтверждений (`mcp-approvals.json`) **трогать не нужно**: устаревшие подтверждения прежних
хешей снимет сам клиент при новом Trust — это его свойство, а не дефект. **Подтверждать Trust
скриптом запрещено**: это обход доверия.

**Действие (б). Открыть НОВУЮ сессию** (список инструментов агента собирается при запуске сессии) и
первым сообщением дать агенту текст:

> Работай строго по файлу `<репозиторий>\IMAGE_CLIENT_CONTINUATION_PROMPT.md`, шаги 0–4

Это одновременно **клиентская приёмка**, закрывающая последний FAIL-операнд паспорта. Перед сеансом
полезно прочитать **блок «ПОПРАВКА 21.09.2026»** в начале этого файла: там оперативные числа
финальной поставки (50 инструментов, `adapter_sha256` = `5303dee8…`) и названные расхождения таблицы
«Что считать успехом».

---

## 5. После приёмки

1. `client-acceptance.json` (результат сценария) и `client-entry-check.json` (переснятая проверка
   входа) — в каталог приёмки: `docs/acceptance/delivery-sketchplane-20260921-plane/` либо
   `scratch/mcp-smoke/sketchplane-20260921/`.
2. Переиздать паспорт **с явными ключами**:
   `python scripts\make-delivery-passport.py --delivery artifacts\publish-sketchplane-20260921 --reports scratch\mcp-smoke\sketchplane-20260921 --out docs\acceptance\delivery-sketchplane-20260921-plane`.
3. Ожидаемое: `client_entry` и `client_acceptance` перестают быть `not_run`, `transport_and_delivery`
   становится **PASS**, вердикт паспорта — **PASS**.
4. **И только тогда** допустима формулировка «выпуск принят» / «release accepted». До этого честная
   формулировка — **«обязательный объём закрыт, транспорт не принят»**.

---

## Приложение А. Что сделано этим нарядом (числа из приборов)

**Работа A — дефект прибора №25.** `scripts/verify-mcp-approval.py`: умолчание `--out` больше не
жёстко прописано в каталог поставки 18.09.2026, а **вычисляется** из источника аудита
(`docs/API_COMPLIANCE.json` → `claimed_actions_check.source`). Источник, каталог которого не
существует или маска стоит в КАТАЛОГЕ, прибор **отклоняет** с названной причиной, а не подставляет
прежний каталог. Батарея прибора добавлена (`--self-test`): **6 из 6**, ЗЕЛЁНАЯ, включая половину
«прежний каталог `delivery-20260918` не возвращается ни на одном входе». Отрицательный контроль
правки: прогон **без** `--out` создал отчёт **только** в
`scratch/mcp-smoke/sketchplane-20260921/mcp-approval-check.json` (список каталогов сверен до и после;
файл 18.09.2026 не тронут — mtime не изменился).

**Работа B — ворота присутствия.** Прибор `scratch/_release_presence_gate.py` (ветвь «вызовы
найдены»), запись — прибор `scratch/_declare_release_routes.py` (слияние + сторож, доказан на
настоящей потере, три половины). Аудит переиздан штатной цепочкой. Изменились **ровно 3 строки**
матрицы, ни одной вне объявлений.

**Работа C — запись клиента.** Одна строка `command`; `args`/`timeout` не тронуты; хеш взят из вывода
прибора.

**Дополнительно найдено и исправлено (не входило в наряд, названо):** поле `meta.date` аудита было
**жёстко прописано** («2026-09-20»), поэтому каждое переиздание называло себя снятым не в тот день —
тот же класс, что отставший COM-снапшот, ради которого в сборщике стоит сторож. Теперь дата берётся с
часов машины в момент сборки: `2026-09-21 (+02:00, часы машины)`.

**Батарея приборов (все — БЕЗ переменных окружения):** `_build_api_compliance.py` rc=0;
`_render_api_compliance.py` rc=0; `_check_compliance_links.py` rc=0 (207/207);
`_verify_matrix_claims.py` rc=0 (71/71, приписок 0); `_verify_action_guard.py` rc=0 (33/33);
`scripts/record-b2-rows.py --check` rc=0; `--self-test` rc=0; `scripts/acceptance-levels.py` rc=0
(COMPLETE); `--self-test` rc=0; `scripts/emit-coverage-matrix.py` rc=0 (целостность OK);
`scripts/project-progress.mjs` rc=0 (100 % · 54/54 · 15/15); `scripts/lint-cjk.py` rc=0;
`scripts/lint-mixed-script.py` rc=0; `scripts/verify-mcp-approval.py --self-test` rc=0 (6/6).

**Продукт не менялся:** ни одного нового инструмента, ни одной правки `src/` и схем. Паспорт не
переиздавался. Сырые отчёты прогонов не редактировались. `KOMPAS.exe` — 0.

---

## Приложение Б. 21.09.2026, 20:5x — повторная сдача после дефекта `JOURNAL-REPLAY-SHARING-VIOLATION`

**Наряд:** `JOURNAL_SHARING_FIX_DEVELOPER_PROMPT.md`. Отчёт — `JOURNAL_SHARING_FIX_REPORT_20260921.md`.
**Что здесь заменено:** §4 этой заметки (поставка и **хеш Trust**), а также команды §5. Всё остальное
в §1–§3 и Приложении А остаётся верным; числа ниже — из приборов этого сеанса.

### Б.1. Почему понадобилась повторная сдача

Клиентская приёмка сеанса 2 (`IMAGE_CLIENT_ACCEPTANCE_RUN2_REPORT_20260921.md`) **не прошла** по
измеренной причине: Хост поставки падал на старте с необработанным `System.IO.IOException`, если
журнал операций уже открыт на запись другим процессом. У клиента условие срабатывает всегда — Хост
предыдущего сеанса жив ещё **23 секунды** после старта нового. Агент оставался с нулём инструментов
`kompas_*` **молча**. Дефект вылечен в продукте: чтение журнала переведено на тот же
`FileShare.ReadWrite`, что и запись; добавлен единственный активный владелец сеанса с именованной
блокировкой и **названным** отказом (`SESSION_OWNER_ACTIVE`), а недоступный журнал даёт
`JOURNAL_UNAVAILABLE` и код выхода 70 вместо тихой работы без механизма безопасности.

### Б.2. Новая поставка (действующие числа)

| Что | Измерено 21.09.2026 |
|---|---|
| Поставка | `artifacts/publish-journalfix-20260921` — **100** файлов, **7.5** МБ, инструментов **50**, схем **50** |
| Проверка поставки | `scripts\verify-delivery.py --delivery artifacts\publish-journalfix-20260921` — **PASS**, `schema_mismatches []`, `repo_drift []`, `problems []` |
| `KompasMcp.Host.dll` | `e8c08d41f9aadbf12c926f3375311121eef826d1c5a3efcdfd271dd063cbfa6b` |
| `KompasMcp.Worker.dll` | `0e28f4b449fc93c177997cd1f99e89742242ad31119a19438d4f4f935893877f` |
| `KompasMcp.Api5Adapter.dll` | `98c59a2e725efeccc6998b17e752ff9632991cfb33f159cec79940e6558b96b9` |
| Прогоны на бинарях этой поставки | full **1086/0**, f08 **301/0**, mania **43/0**, dep **80/0**, image **37/0** отказов — `scratch/mcp-smoke/delivery-journalfix-20260921/` |
| Пробы журнала | P1–P6 — **все PASS** (`docs/acceptance/image/journal-sharing-probe.json`); исход второго Хоста **более не `0xE0434352`** |
| Батарея приборов и линтеры | rc=0 все, без переменных окружения; сторож действий **33/33**, притязания **71/71**, ссылки **207/207** |
| Паспорт | **НЕ переиздан** — наряд §0.4 запрещает издавать его до повторной приёмки; состояние `not_run` сохранено |

### Б.3. Новый хеш для Trust — и почему Trust нужен заново

Запись `kompas` в `<профиль>/.workbuddy-ai/mcp.json` переведена на новую поставку (изменена одна
строка `command`; резервная копия — `scratch/client-config-backup-20260921-journalfix.json`).
**Хеш записи снят прибором `scripts\verify-mcp-approval.py`:**

```
6fa5a02e130c1aad0c0cd29caaf7b66357c98516cc168cfafcda678b58573f79
```

**Причина повторного Trust:** хеш записи складывается из команды, аргументов и имён переменных
окружения. Сменился каталог поставки — сменилась команда — **сменился хеш**, и прежнее подтверждение
(`b4962263…`) на новую запись не распространяется. Это **одно нажатие пользователя**, а не дефект.
Состояние по прибору: **A01–A03 PASS**, **A04/A05 FAIL** — «ожидает Trust» (пять прежних
подтверждений выданы под другими хешами).

### Б.4. Состояние после сеанса 2 — названо

Сеанс 2 закрыт **отказом**: 0 инструментов у агента, проверка входа при этом PASS 15/0 (поставка была
исправна, транспорт — нет). Отчёт сеанса 2 **не переписан**. Паспорт `delivery-sketchplane-20260921-plane`
оставлен как есть, с `transport_and_delivery` **FAIL**. Честная формулировка на сейчас: **«обязательный
объём закрыт, транспорт не принят»**. «Выпуск принят» / «release accepted» — **недопустимо** до
итогового PASS.

### Б.5. Что должен сделать пользователь

Прежние **два действия**, но под **новым** хешем: (а) **Trust** в интерфейсе клиента под хешем
`6fa5a02e…`; (б) **новая сессия** по `IMAGE_CLIENT_CONTINUATION_PROMPT.md`, шаги 0–4 (сеанс 3). После
PASS приёмки — `client-acceptance.json` в каталог отчётов, переиздание паспорта **уже исправленным**
генератором ключами `--delivery artifacts\publish-journalfix-20260921 --reports
scratch\mcp-smoke\delivery-journalfix-20260921 --out docs\acceptance\delivery-journalfix-20260921-journal`,
`transport_and_delivery` PASS, итоговый вердикт PASS.

### Б.6. Дефекты, найденные по ходу (названы, не спрятаны)

Противоречие наряда `PASSPORT-REQUIRED-BEFORE-ISSUE` (приборы требовали паспорт, который наряд
запрещает издавать) — разрешено в пользу запрета: опознание поставки берётся её измерением, паспорт
подключается, если издан. `mcp-smoke.py --help` запускает полный прогон вместо справки. Живой Хост
**прежней** поставки (pid 38508) на машине — назван, не снят: он принадлежит клиентскому сеансу.
Комментарий `NamedFileLock.cs` называет одно измерение (`381/400`) как константу, тогда как величина
непостоянна (**381**, **350**, **366** из 400) — правка откачена осознанно: она меняет
`KompasMcp.Domain.dll` и, значит, уже измеренную поставку. Подробности — §5 отчёта.

## Приложение В. 21.09.2026, 21:1x — СЕАНС 3: клиентская приёмка ПРОЙДЕНА, паспорт переиздан

**Разделы Б.4 и Б.5 выше описывают состояние ДО сеанса 3 и не переписаны.** Ниже — то, что изменилось
после него. Отчёт сеанса 3 — `IMAGE_CLIENT_ACCEPTANCE_RUN3_REPORT_20260921.md`.

| Поле | Значение |
|---|---|
| Сеанс | `ea6ec245-48df-43ec-8fb0-940604eb7a22`, WorkBuddy AI 5.5.2 (сборка `910352f0…`) |
| Хост | поднят **самим клиентом**: pid **39816**, `state serving`, старт `2026-09-21T18:41:57Z`; Worker pid **31328**. Доверие выдано `18:41:22Z` — **на 35 с раньше запуска** |
| Инструменты у агента | **50 из 50** (индекс отложенных инструментов, а не статус «connected») |
| Вызовы клиента | **150** до Хоста (138 succeeded / 12 failed, **все 12 намеренные**) + 1 отклонён клиентом до сервера = **151** попытка |
| Image-блок | **ПРОКИНУТ**: PNG `581×676`, **54127** байт, `image_base64` с магией `89504e470d0a1a0a`; агент снимок прочитал |
| Отрицательные пробы | **шесть**, все названы, ни одной image-блока |
| Пользовательские модели | не изменены (корень моделей заказчика — ни одной записи в окне сеанса); документы сеанса закрыты; **`KOMPAS.exe` — 0** |
| Проверка входа | переснята: **PASS, 15 строк, 0 отказов** (было FAIL: 36 против 49); `E03b` PASS, `E07` **50** |
| Запись клиента | хеш **`6fa5a02e130c1aad0c0cd29caaf7b66357c98516cc168cfafcda678b58573f79`** — **A01–A04 PASS**, A05 FAIL (строгость операнда: 5 устаревших ключей) |
| **Паспорт** | **ПЕРЕИЗДАН** явными ключами → `docs/acceptance/delivery-journalfix-20260921-journal/delivery-passport.json`: `acceptance` **PASS**, `fully_ready` **true**, уровни **PASS / PASS / COMPLETE / PASS**, `measured_binaries.divergences []` |
| Формулировка | **«выпуск принят» / «release accepted»** — теперь допустима. Прежняя («обязательный объём закрыт, транспорт не принят») относилась к состоянию до сеанса 3 |

**Что осталось названным, а не закрытым.** Попытка скругления R3 по **всему набору 13 рёбер** агентом
не переигрывалась — ожидаемый `GEOMETRY_FAILED` измерен прибором на этой же поставке (`mania.json`,
`MANIA.10.create`). Остаток мониторинга `MANIA.*` (π·7²·5 = 769.690200) **не воспроизвёлся**, и
клиентский сеанс эту строку не упражнял. Расхождение правила вердикта: объявленное
(`make-delivery-passport.py:780` — только package + entry) **слабее** вычисляемого
(`acceptance-levels.py:182–206` — требует клиентской приёмки) — передано владельцу репозитория,
генератор не правился. Дефект **моей пробы** (контур «дуга + 2 радиуса» при размахе, кратном 360°,
вырождается → `GEOMETRY_FAILED`) назван и переигран дугой без радиусов.
